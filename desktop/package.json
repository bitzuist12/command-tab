{
  "name": "command-tab-wa-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Command Tab WhatsApp — local Tauri app (read-only digest → hosted summarizer).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
